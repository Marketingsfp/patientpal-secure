// Stub temporário — arquivo ficou corrompido por conflitos de merge.
// A agenda V2 é experimental (atrás de feature flag) e não é usada em
// produção. Reintegrar quando a Fase F for retomada.
export function NovoAgendamentoWizard(_props: {
  open: boolean;
  onOpenChange: (v: boolean) => void;
  initial?: unknown;
}) {
  return null;
}
