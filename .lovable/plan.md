No `src/lib/print-carne.ts`, dentro do `.capa-grid`, transformar as duas células "Início" e "Dia de vencimento" em colunas flex que empurram seu conteúdo para a base (`display:flex; flex-direction:column; justify-content:flex-end`), de modo que a linha do valor fique alinhada horizontalmente com "Total do contrato" — que é o último item da coluna da direita (Parcelas → Valor mensal → Total do contrato).

Sem mudanças em lógica, dados ou outros campos.